node_modules
dist
# Built GitHub Action bundle + vendored language server. Generated by
# `npm run build:action`; force-added into release tags by the release workflow,
# so it stays out of ordinary commits (see .github/workflows/release.yml).
action
*.log
.DS_Store
